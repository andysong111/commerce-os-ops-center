# Legacy SEO focused CI validation

This branch exists only to run the focused legacy SEO registration regression workflow against the current main baseline. The source/test change on this branch verifies explicit exclusion precedence while the workflow exercises the full focused suite.
