Temporary validation note for the compact legacy SEO list loading fix. The production page was repeatedly downloading large checkpoint payloads during polling; the fix routes GET list reads to a compact endpoint while preserving POST actions on the full endpoint.
